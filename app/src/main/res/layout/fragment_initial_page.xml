<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".fragments.InitialPageFragment"
    tools:showIn="@layout/activity_initial_page">

    <TextView
        android:id="@+id/lblText"
        android:text="Welcome to QuizTime!"
        style="@style/headerStyle"
        android:layout_margin="20dp"/>
    
    <Button
        android:id="@+id/btnStartNewGame"
        android:text="Start new game"
        android:layout_marginTop="120dip"
        android:layout_below="@id/lblText"
        style="@style/btnStyles"
        android:onClick="goToStartNewGameActivity"/>

    <Button
        android:id="@+id/btnCreateNewQuestion"
        android:text="Create new"
        android:layout_below="@id/btnStartNewGame"
        android:layout_marginTop="30dip"
        style="@style/btnStyles"
        android:onClick="goToCreateNewQuestionOrCategory"/>

</RelativeLayout>
